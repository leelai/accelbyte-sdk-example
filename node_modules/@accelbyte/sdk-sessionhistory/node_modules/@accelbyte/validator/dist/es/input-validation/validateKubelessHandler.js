import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateKubelessHandlerErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const validateKubelessHandler = (value, { isRequired = true } = {}) => {
  const REGEX = "[a-zA-Z0-9_]$";
  const MAX_HANDLER_LENGTH = 256;
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateKubelessHandlerErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateKubelessHandlerErrorType.enum.invalidFormat;
  }
  return validateLength(value, { max: MAX_HANDLER_LENGTH });
};

export { ValidateKubelessHandlerErrorType, validateKubelessHandler };
//# sourceMappingURL=validateKubelessHandler.js.map
//# sourceMappingURL=validateKubelessHandler.js.map