import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { MAX_SHORT_TEXT_LENGTH } from './constant/numbers';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateDatabaseNameErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const validateDatabaseName = (value, { maxLength = MAX_SHORT_TEXT_LENGTH, isRequired = true } = {}) => {
  const REGEX = "[a-zA-Z0-9_]$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateDatabaseNameErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateDatabaseNameErrorType.enum.invalidFormat;
  }
  return validateLength(value, { max: maxLength });
};

export { ValidateDatabaseNameErrorType, validateDatabaseName };
//# sourceMappingURL=validateDatabaseName.js.map
//# sourceMappingURL=validateDatabaseName.js.map