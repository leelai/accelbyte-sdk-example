import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateSeasonPassCodeErrorType = z.enum([
  ...ValidateLengthErrorType.options,
  CommonValidationErrorType.enum.invalidFormat,
  CommonValidationErrorType.enum.alreadyUsed
]);
const validateSeasonPassCode = (value, { isRequired = true } = {}) => {
  const REGEX = "^[a-z0-9]+([_:-]{1}[a-z0-9]+)*$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateSeasonPassCodeErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateSeasonPassCodeErrorType.enum.invalidFormat;
  }
  return validateLength(value);
};

export { ValidateSeasonPassCodeErrorType, validateSeasonPassCode };
//# sourceMappingURL=validateSeasonPassCode.js.map
//# sourceMappingURL=validateSeasonPassCode.js.map