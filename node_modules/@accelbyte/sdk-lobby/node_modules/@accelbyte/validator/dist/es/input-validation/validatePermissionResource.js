import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidatePermissionResourceErrorType = z.enum([
  ...ValidateLengthErrorType.options,
  CommonValidationErrorType.enum.invalidFormat
]);
const validatePermissionResource = (value, { isRequired = true } = {}) => {
  const REGEX = "^[A-Z]+([:]{1}([A-Z]+|(({[a-zA-Z]+})|[a-zA-Z0-9]+|\\*)))*$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidatePermissionResourceErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidatePermissionResourceErrorType.enum.invalidFormat;
  }
  return validateLength(value);
};

export { ValidatePermissionResourceErrorType, validatePermissionResource };
//# sourceMappingURL=validatePermissionResource.js.map
//# sourceMappingURL=validatePermissionResource.js.map