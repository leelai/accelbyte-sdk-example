import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateKubelessNameErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const validateKubelessName = (value, { isRequired = true } = {}) => {
  const REGEX = "^[a-zA-Z]+(?:([a-zA-Z0-9]+)[-]*[a-zA-Z0-9]+)*$";
  const MAX_KUBELESS_NAME_LENGTH = 64;
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateKubelessNameErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateKubelessNameErrorType.enum.invalidFormat;
  }
  return validateLength(value, { max: MAX_KUBELESS_NAME_LENGTH });
};

export { ValidateKubelessNameErrorType, validateKubelessName };
//# sourceMappingURL=validateKubelessName.js.map
//# sourceMappingURL=validateKubelessName.js.map