import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateCpuSizeNonNomadErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const validateCpuSizeNonNomad = (value, { isRequired = true } = {}) => {
  const MAX_LENGTH = 8;
  const REGEX = "^[0-9]+m$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateCpuSizeNonNomadErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateCpuSizeNonNomadErrorType.enum.invalidFormat;
  }
  return validateLength(value, { max: MAX_LENGTH });
};

export { ValidateCpuSizeNonNomadErrorType, validateCpuSizeNonNomad };
//# sourceMappingURL=validateCpuSizeNonNomad.js.map
//# sourceMappingURL=validateCpuSizeNonNomad.js.map