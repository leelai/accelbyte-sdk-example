import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';

const ValidateOrderNumberErrorType = z.enum([CommonValidationErrorType.enum.empty, CommonValidationErrorType.enum.invalidFormat]);
const validateOrderNumber = (value, { isRequired = true } = {}) => {
  const REGEX = "^O[0-9]{16,}$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateOrderNumberErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateOrderNumberErrorType.enum.invalidFormat;
  }
  return null;
};

export { ValidateOrderNumberErrorType, validateOrderNumber };
//# sourceMappingURL=validateOrderNumber.js.map
//# sourceMappingURL=validateOrderNumber.js.map