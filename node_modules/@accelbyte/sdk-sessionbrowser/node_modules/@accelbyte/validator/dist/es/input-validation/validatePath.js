import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidatePathErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const validatePath = (value, { isRequired = true, maxLength } = {}) => {
  const REGEX = "^(\\/[a-zA-Z0-9_-]+)+$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidatePathErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidatePathErrorType.enum.invalidFormat;
  }
  return validateLength(value, { max: maxLength });
};

export { ValidatePathErrorType, validatePath };
//# sourceMappingURL=validatePath.js.map
//# sourceMappingURL=validatePath.js.map