import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateCpuSizeErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const validateCpuSize = (value, { isRequired = true } = {}) => {
  const MAX_LENGTH = 8;
  const REGEX = "^[0-9]+$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateCpuSizeErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateCpuSizeErrorType.enum.invalidFormat;
  }
  return validateLength(value, { max: MAX_LENGTH });
};

export { ValidateCpuSizeErrorType, validateCpuSize };
//# sourceMappingURL=validateCpuSize.js.map
//# sourceMappingURL=validateCpuSize.js.map