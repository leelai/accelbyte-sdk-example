import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateTemplateSlugErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const validateTemplateSlug = (value, { isRequired = true } = {}) => {
  const REGEX = "^[a-zA-Z]+([-]{1}[a-zA-Z]+)*$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateTemplateSlugErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateTemplateSlugErrorType.enum.invalidFormat;
  }
  return validateLength(value);
};

export { ValidateTemplateSlugErrorType, validateTemplateSlug };
//# sourceMappingURL=validateTemplateSlug.js.map
//# sourceMappingURL=validateTemplateSlug.js.map