import { RolePermission } from './role.js';

interface ExtensionSubModule {
    id: string;
    title: {
        [x: string]: string;
    };
    isEnabled: boolean;
    permission: RolePermission;
    allowedNamespaces?: string[];
}
interface ExtensionModule {
    id: string;
    title: {
        [x: string]: string;
    };
    icon: string;
    isEnabled: boolean;
    subModules: ExtensionSubModule[];
    permission?: RolePermission;
}
interface ExtensionManifest {
    name: string;
    author: string;
    modules: ExtensionModule[];
}
declare class ExtensionManifestDecodeError extends Error {
    constructor(message?: string);
}

export { type ExtensionManifest, ExtensionManifestDecodeError, type ExtensionModule, type ExtensionSubModule };
