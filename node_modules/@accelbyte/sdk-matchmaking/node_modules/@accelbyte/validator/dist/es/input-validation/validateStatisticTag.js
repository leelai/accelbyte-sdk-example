import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateStatisticTagErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const validateStatisticTag = (value, { isRequired = true } = {}) => {
  const MAX_LENGTH = 36;
  const REGEX = "^[a-zA-Z0-9]+([_]{1}[a-zA-Z0-9]+)*$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateStatisticTagErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateStatisticTagErrorType.enum.invalidFormat;
  }
  return validateLength(value, { max: MAX_LENGTH });
};

export { ValidateStatisticTagErrorType, validateStatisticTag };
//# sourceMappingURL=validateStatisticTag.js.map
//# sourceMappingURL=validateStatisticTag.js.map