import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateEventPayloadErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const validateEventPayload = (value, { maxLength = 255, isRequired = true } = {}) => {
  const REGEX = "^\\b((?=[\\w])[\\w]+(\\.[\\w]+)*:)+[\\w]{1,255}\\b$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateEventPayloadErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateEventPayloadErrorType.enum.invalidFormat;
  }
  return validateLength(value, { max: maxLength });
};

export { ValidateEventPayloadErrorType, validateEventPayload };
//# sourceMappingURL=validateEventPayload.js.map
//# sourceMappingURL=validateEventPayload.js.map