import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';

const ValidateBase64ErrorType = z.enum([CommonValidationErrorType.enum.empty, CommonValidationErrorType.enum.invalidFormat]);
const validateBase64 = (value, { isRequired = true } = {}) => {
  const REGEX = "^[A-Za-z0-9+/]+={0,2}$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateBase64ErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateBase64ErrorType.enum.invalidFormat;
  }
  return null;
};

export { ValidateBase64ErrorType, validateBase64 };
//# sourceMappingURL=validateBase64.js.map
//# sourceMappingURL=validateBase64.js.map