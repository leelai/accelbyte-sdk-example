import { z } from 'zod';

declare const ValidateRegexErrorType: z.ZodEnum<["invalidFormat"]>;
type ValidateRegexErrorType = z.infer<typeof ValidateRegexErrorType>;
interface ValidateRegexOptions {
    allowUnicode?: boolean;
}
declare const validateRegex: (value: string, regex: string, { allowUnicode }?: ValidateRegexOptions) => "invalidFormat" | null;

export { ValidateRegexErrorType, type ValidateRegexOptions, validateRegex };
