import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateTopicErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const validateTopic = (value, { isRequired = true } = {}) => {
  const REGEX = "^[A-Z]+([_]{1}[A-Z]+)*$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateTopicErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateTopicErrorType.enum.invalidFormat;
  }
  return validateLength(value);
};

export { ValidateTopicErrorType, validateTopic };
//# sourceMappingURL=validateTopic.js.map
//# sourceMappingURL=validateTopic.js.map