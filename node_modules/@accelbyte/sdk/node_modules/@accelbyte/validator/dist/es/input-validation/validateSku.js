import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateSkuErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const MAXIMUM_SKU_LENGTH = 32;
const validateSku = (value, { isRequired = true, maxLength = MAXIMUM_SKU_LENGTH } = {}) => {
  const REGEX = "^[a-zA-Z0-9]+([_:-]{1}[a-zA-Z0-9]+)*$|^$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateSkuErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateSkuErrorType.enum.invalidFormat;
  }
  return validateLength(value, { max: maxLength });
};

export { ValidateSkuErrorType, validateSku };
//# sourceMappingURL=validateSku.js.map
//# sourceMappingURL=validateSku.js.map