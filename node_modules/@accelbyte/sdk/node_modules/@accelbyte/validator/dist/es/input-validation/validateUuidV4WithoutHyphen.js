import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';

const ValidateUuidV4WithoutHyphenErrorType = z.enum([
  CommonValidationErrorType.enum.empty,
  CommonValidationErrorType.enum.invalidFormat
]);
const validateUuidV4WithoutHyphen = (value, { isRequired = true } = {}) => {
  const REGEX = "^[0-9a-f]{12}4[0-9a-f]{3}[89ab][0-9a-f]{15}$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateUuidV4WithoutHyphenErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateUuidV4WithoutHyphenErrorType.enum.invalidFormat;
  }
  return null;
};

export { ValidateUuidV4WithoutHyphenErrorType, validateUuidV4WithoutHyphen };
//# sourceMappingURL=validateUuidV4WithoutHyphen.js.map
//# sourceMappingURL=validateUuidV4WithoutHyphen.js.map