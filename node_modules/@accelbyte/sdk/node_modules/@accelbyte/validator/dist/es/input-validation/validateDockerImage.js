import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateDockerImageErrorType = z.enum([...ValidateLengthErrorType.options, CommonValidationErrorType.enum.invalidFormat]);
const validateDockerImage = (value, { isRequired = true } = {}) => {
  const REGEX = "^[a-z0-9]+([.\\/_-]{1}[a-z0-9]+)*([:]{1}([0-9a-z]+([.-]{1}[0-9a-z]+)*)+)?$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateDockerImageErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateDockerImageErrorType.enum.invalidFormat;
  }
  return validateLength(value);
};

export { ValidateDockerImageErrorType, validateDockerImage };
//# sourceMappingURL=validateDockerImage.js.map
//# sourceMappingURL=validateDockerImage.js.map