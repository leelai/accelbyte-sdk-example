import isEmpty from 'validator/lib/isEmpty.js';
import matches from 'validator/lib/matches.js';
import { z } from 'zod';
import { CommonValidationErrorType } from './constant/errorType';
import { ValidateLengthErrorType, validateLength } from './validateLength';

const ValidateStatisticCodeErrorType = z.enum([
  ...ValidateLengthErrorType.options,
  CommonValidationErrorType.enum.invalidFormat,
  CommonValidationErrorType.enum.alreadyUsed
]);
const validateStatisticCode = (value, { isRequired = true } = {}) => {
  const REGEX = "^[a-z0-9]+([-]{0,1}[a-z0-9]+)*$";
  if (isEmpty(value)) {
    if (!isRequired) {
      return null;
    }
    return ValidateStatisticCodeErrorType.enum.empty;
  }
  if (!matches(value, REGEX)) {
    return ValidateStatisticCodeErrorType.enum.invalidFormat;
  }
  return validateLength(value);
};

export { ValidateStatisticCodeErrorType, validateStatisticCode };
//# sourceMappingURL=validateStatisticCode.js.map
//# sourceMappingURL=validateStatisticCode.js.map